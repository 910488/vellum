# Overflow recovery scenario

First run `python emit_context.py` without redirecting or truncating its output.
Use the repeated rule in that tool output to implement `solve` in `solution.py`,
then run a focused local check.
