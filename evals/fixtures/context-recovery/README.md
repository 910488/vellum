# Context recovery fixtures

Mechanism evidence is the in-process Port B runner (huge tool results,
prune→remeasure, overflow retry, idle/success reset). `chunk_text` is only a
live coding wrapper and is **not** a context-overflow hard gate.
