# Bounded continuation scenario

Your first action must be an `update_plan` call with these pending steps:

1. inspect `solution.py`;
2. implement `pending_total` as the sum of non-negative values;
3. run a focused check.

Then complete every pending step. Keep the public function name unchanged.
